# WS-022 Handoff — Founder entitlements (2026-07-04)

**Summary:** Server-authoritative entitlement resolution keyed by user ID.
Admin comes from a `user_roles` DB row (email allowlist demoted to deprecated
fallback); an `internal_unlimited` `user_plans` row resolves to admin-tier
unlimited limits. Founder seed migration written, NOT applied.
Executed by Fable directly — three delegated worker attempts each stalled
without producing work (deviation from the ≤3-workers delegation plan,
recorded here).

**Follow-up verification pass (this update):** re-ran the packet against
the branch as an independent check. Found and fixed one real defect in the
migration (see below); re-verified `tsc`/`vitest`; pushed a second commit.

- **Branch/commits:** `fix/founder-entitlements` @ `4595013` (original), `4ba4269` (fix, this pass) — both pushed, base `b49f4ac`
- **Files:** `api/_lib/entitlements.ts`, `api/_lib/plan-limits.ts`,
  `api/_lib/entitlements.test.ts` (new), `supabase/migrations/20260704091500_internal_unlimited_plan.sql` (new; amended in `4ba4269`)
- **Fix in `4ba4269`:** the migration's `user_plans` INSERT sets
  `plan = 'internal_unlimited'`, but the pre-existing `user_plans_plan_check`
  CHECK constraint (from `20260312010000_user_plans.sql`) only allowed
  `'free' | 'pro' | 'studio'` — applying the migration as originally written
  would have failed at that INSERT. Added an `ALTER TABLE ... DROP/ADD
  CONSTRAINT` step widening the allowed values to include
  `'internal_unlimited'`, with a matching reversal statement appended to the
  documented rollback block.
- **Tests:** 8 new (`api/_lib/entitlements.test.ts`), all green; full wavio suite 230
  tests / 24 files passing, zero failures, re-run after the `4ba4269` fix;
  `npx tsc --noEmit` clean (re-verified after the fix too).
- **Acceptance:** founder id → admin+unlimited ✅ · email change never revokes ✅ ·
  Free user unaffected (26/500MB fallback untouched) ✅ · client state cannot
  grant (id-keyed, RLS-locked service-role-only table) ✅ · unauthenticated →
  zero-trust default ✅ · usage recording unchanged ✅ · no founder email
  introduced by this task in client code ✅ · migration additive+reversible
  (now with the constraint fix), NOT applied ✅
- **Blockers:** migration needs founder approval before applying anywhere;
  until then admin still flows through the email fallback in production.
- **Remaining risk:**
  - `user_plans.status`/`period_end` semantics assumed from existing
    `getUserPlan`; verify the production `plan_limits` schema columns match
    the INSERT before applying.
  - Pre-existing, out-of-scope finding: `src/lib/subscription.ts` (~line 47)
    has its own separate, older `ADMIN_EMAILS = ['rishmanx@gmail.com']`
    constant that grants a client-side "premium" tier override — unrelated
    to `api/_lib/entitlements.ts`, not touched by WS-022 (out of its stated
    file scope), but it does violate the spirit of "never hardcode founder
    email in client code." A follow-up task has been flagged to remove it
    and route that check through the server-side `getEntitlement()` endpoint
    instead.
  - `user_roles` has RLS enabled with no policies (service-role-only by
    omission) — confirm this is the intended posture during migration review.
- **Decision requested:** founder approval to apply the migration (staging first,
  now including the constraint-widening step), then merge `fix/founder-entitlements`;
  and a decision on prioritizing the `src/lib/subscription.ts` cleanup follow-up.
