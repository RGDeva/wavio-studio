# WS-015 · Dead-code sweep — Handoff (2026-07-02)

## Summary
Removed dead code left by the Dashboard row simplification and recent merges. All removals verified unused by grep before deletion; zero behavior change.

- `src/pages/Dashboard.tsx`: removed unused lucide-react icon imports `Link`, `Copy`, `Check`, `Package`, `ExternalLink`; removed unused `getDawColor` import and the unused `dawColor` local in `ProjectRow`.
- `src/pages/FoldersPage.tsx`: removed unused lucide-react `Check` import, unused `DAW_FOLDER_HINTS` constant, unused `excludingFolder` state, and dead `handleExcludeSubfolder` helper (never referenced anywhere in src/).
- `src/pages/SettingsPage.tsx` and `src/pages/LinksPage.tsx` were inspected; no dead symbols found — untouched.

## Branch & commits
- Branch: `chore/dead-code-sweep` (base: `feature/ableton-daw-companion`), pushed to origin.
- Commit: `3932452f` — "chore: remove unused imports left by dashboard simplification" (both files; FoldersPage was already staged, so a planned second commit collapsed into this one).

## Files changed
- src/pages/Dashboard.tsx (3 changed lines)
- src/pages/FoldersPage.tsx (27 changed lines)
- Total: 2 files, 3 insertions(+), 29 deletions(-) — well under the 120-line cap.

## Gate results (run from worktree root ~/wavi-worktrees/dead-code)
- `npx tsc -p tsconfig.electron.json` — PASS
- `npx tsc --noEmit -p .` — PASS
- `npx vitest run` — PASS: **Test Files 25 passed (25), Tests 434 passed (434)** (matches 434 baseline; better-sqlite3 suites ran, none skipped)
- `npm run build` — PASS (vite build completed, ✓ built in 1.45s)

## Deviations
- None in scope. Process note: the two-file change landed as a single commit instead of two (FoldersPage was pre-staged); message references the dashboard simplification but the commit also covers the FoldersPage cleanup.
- No tsconfig changes committed (`--noUnusedLocals` used only as a transient CLI check).

## Remaining risk
- None identified for the changes made. All removed symbols had zero usages; full gates green post-removal.

## Blocker flagged (not fixed — out of conservative scope)
- `src/pages/Dashboard.tsx`: the `importStatus` state (`useState` at line 38) is set in several places inside `handleDrop` (e.g. "Importing N files…", "Imported N files", "Import failed") but its value is never read/rendered anywhere in the JSX — `npx tsc --noEmit -p . --noUnusedLocals` flags it as TS6133. Removing it cleanly would require deleting the `setImportStatus(...)` calls inside `handleDrop`, which counts as a logic edit (removing in-flight import-status behavior that may be intended for a not-yet-wired UI element), so it was left untouched per the "no logic edits" rule and is flagged here instead of forced through.
